name: my-demo

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  name: Arjun

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "hi hello, I am $name"

  second_job:
    runs-on: ubuntu-latest
    env:
      job: jobless
    steps:
      - name: first step
        run: echo "hi, I am $name and I am $job"

  third_job:
     runs-on: ubuntu-latest
     steps:
       - name: show the repository
         run: echo "hi, this is the repo path"${{ github.repository }}
